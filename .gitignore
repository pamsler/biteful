# ============================================================================
# Biteful - GitHub Repository Configuration
# ============================================================================
# This repository only contains deployment configuration files.
# All source code remains private and is only distributed via Docker Hub.
#
# Public files:
#   - .env.production.example (environment variables template)
#   - docker-compose.production.yml (Docker deployment configuration)
# ============================================================================

# Ignore everything by default
*
*/

# Allow only specific deployment files
!.gitignore
!.env.production.example
!docker-compose.production.yml
